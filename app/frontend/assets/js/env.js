// Overridable API origin. Leave empty for same-origin deployment (the default:
// Express serves this frontend and the API from one host, e.g. on Render).
// When hosting this frontend standalone (e.g. on Vercel, separate from the
// Express backend), set this to the backend's absolute origin + /api/v1.
window.TBT_API_BASE = '';
