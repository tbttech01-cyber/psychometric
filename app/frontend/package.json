{
  "name": "tbt-candidate-frontend",
  "private": true,
  "scripts": {
    "build": "tailwindcss -i ./src/tailwind.css -o ./assets/css/tailwind.css --minify && node build-env.js"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
